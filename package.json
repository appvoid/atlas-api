{
  "name": "atlas",
  "version": "1.0.0",
  "description": "API de clasificacion y gestion de tickets con Express, SQLite, Valibot y CrispEmbed.",
  "private": true,
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node src/servidor.js",
    "dev": "node --watch src/servidor.js",
    "test": "node --test tests/*.test.js",
    "build": "node -e \"console.log('Atlas usa JavaScript puro; no hay paso de build.')\"",
    "build:crispembed": "node scripts/build-crispembed.js"
  },
  "dependencies": {
    "better-sqlite3": "^12.9.0",
    "express": "^5.1.0",
    "valibot": "^1.4.0"
  }
}
